<%if @id == "2"%>
<div class ="ui small images" id="grid">
<%else%>
<div class="ui center aligned items" id="list">
<%end%>
  <%= render partial: 'category', collection: @posts, locals: {show_link: false}%>
</div>